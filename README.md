# 官途算略 - 古代职场身份映射指南

一款趣味职场身份映射工具。输入你的现代职业和薪资，通过《宛署杂记》《醒贪简要录》等历史文献数据，换算成明朝万历年间的购买力，揭示你在古代的官职品阶、生活水平和趣味判词。

## ✨ 功能特性

- 📊 **薪资购买力换算**：基于历史文献的大数据核算，精确换算购买力
- 🏛️ **古代官职对应**：智能匹配你的职业类型与古代官职
- 📜 **趣味生成判词**：根据个人履历生成专属的古风判词
- 🎭 **穿越生活模拟**：体验古代不同阶层的一天生活
- 🎨 **古风界面设计**：宣纸质感、印章元素、传统配色

## 🚀 快速开始

### 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 📦 Vercel 部署教程

### 方式一：通过 Vercel Dashboard（推荐）

1. **注册/登录 Vercel**
   - 访问 [Vercel.com](https://vercel.com)
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 选择你的 GitHub 仓库
   - 如果仓库未授权，点击 "Adjust GitHub App Permissions" 添加权限

3. **配置项目**
   ```
   Framework Preset: Vite
   Root Directory: ./
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待构建完成（约 1-2 分钟）
   - 获得默认域名：`your-project-name.vercel.app`

### 方式二：通过 Vercel CLI

```bash
# 全局安装 Vercel CLI
npm install -g vercel

# 登录 Vercel
vercel login

# 部署项目
vercel

# 部署到生产环境
vercel --prod
```

### 配置自定义域名：gt.kw-aigc.cn

#### 步骤 1：在 Vercel 添加域名

1. 进入项目 Dashboard
2. 点击 "Settings" → "Domains"
3. 输入 `gt.kw-aigc.cn`
4. 点击 "Add"

#### 步骤 2：配置 DNS 记录

Vercel 会提示需要添加的 DNS 记录，通常是以下两种方式之一：

**方式 A：使用 CNAME 记录（推荐）**

在你的 DNS 服务商（阿里云/腾讯云/Cloudflare 等）添加：

```
类型: CNAME
主机记录: gt
记录值: cname.vercel-dns.com
TTL: 600（或默认）
```

**方式 B：使用 A 记录**

```
类型: A
主机记录: gt
记录值: 76.76.21.21
TTL: 600（或默认）
```

#### 步骤 3：验证域名

1. DNS 记录添加后，等待 1-10 分钟生效
2. 返回 Vercel Dashboard，等待域名验证成功
3. SSL 证书会自动配置（通常在几分钟内完成）

#### 步骤 4：设置为主域名（可选）

如果你想让 `gt.kw-aigc.cn` 成为主域名：

1. 在 Domains 页面找到 `gt.kw-aigc.cn`
2. 点击右侧的 "..." → "Set as Primary Domain"

## 🔧 配置文件说明

### vercel.json（可选）

如果需要自定义 Vercel 配置，可以在项目根目录创建 `vercel.json`：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    },
    {
      "source": "/(.*)\\.(jpg|jpeg|png|gif|webp|svg|ico)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

## 🛠️ 技术栈

- **框架**：React 18 + TypeScript
- **构建工具**：Vite
- **样式方案**：Tailwind CSS
- **UI 组件**：Radix UI
- **图标库**：Lucide React
- **字体**：Noto Serif SC（思源宋体）+ Ma Shan Zheng（马善政楷书）

## 📂 项目结构

```
calculate-your-salary/
├── public/               # 静态资源
│   ├── logo.svg         # 网站图标
│   ├── og-image.svg     # 社交分享卡片
│   └── *.webp           # 物品图片资源
├── src/
│   ├── components/      # 公共组件
│   │   ├── Footer.tsx   # 页脚（官网链接、二维码）
│   │   └── ui/          # UI 组件库
│   ├── sections/        # 页面区块
│   │   ├── InputForm.tsx    # 表单页
│   │   ├── Calculating.tsx  # 加载页
│   │   └── Result.tsx       # 结果页
│   ├── data/            # 数据与逻辑
│   │   ├── levelMapping.ts     # 官职映射算法
│   │   ├── sceneGenerator.ts  # 场景生成器
│   │   └── sceneContent.ts    # 场景内容库
│   ├── types/           # TypeScript 类型定义
│   ├── index.css        # 全局样式
│   └── main.tsx         # 入口文件
├── index.html           # HTML 模板（含 SEO）
├── vite.config.ts       # Vite 配置
└── tailwind.config.js   # Tailwind 配置
```

## 🎨 样式定制

项目采用古风主题配色，主要色彩：

- **宣纸黄**：`#F5E6C8`
- **朱砂红**：`#C9372C`
- **墨黑**：`#1A1A1A`
- **靛蓝**：`#2E4A62`
- **金色**：`#C9A961`

## 📱 响应式设计

- 移动端优先的响应式布局
- 触摸友好的交互设计
- 适配各种屏幕尺寸（320px ~ 4K）
- 统一的字体系统（详见 [TYPOGRAPHY.md](./TYPOGRAPHY.md)）

## 🔍 SEO 优化

- 完整的 Meta 标签配置
- Open Graph 协议支持
- 自定义 Favicon 和 Logo
- 语义化 HTML 结构

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 👨‍💻 作者

Kevin Young - [www.kw-aigc.cn](https://www.kw-aigc.cn)

## 🙏 致谢

- 历史文献：《宛署杂记》《醒贪简要录》
- 字体：Google Fonts（Noto Serif SC, Ma Shan Zheng）
- UI 组件：Radix UI, shadcn/ui

---

**快来测算你的古代身份吧！** 🎉
